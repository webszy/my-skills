interface:
  display_name: "cdp"
  short_description: "Clarifies requirements, then chooses risk-based coding workflow"
  default_prompt: "Use $cdp to clarify vague requirements, then choose the lightest safe workflow for this coding task."
policy:
  allow_implicit_invocation: false
